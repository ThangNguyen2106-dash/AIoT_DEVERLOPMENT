/* Toàn bộ ánh xạ phần cứng của board nằm ở đây — không rải #define pin ra các file khác. */
#pragma once

/* I2C */
#define BOARD_I2C_PORT        I2C_NUM_0
#define BOARD_I2C_SDA_GPIO    8
#define BOARD_I2C_SCL_GPIO    9
#define BOARD_I2C_FREQ_HZ     400000

/* Trạng thái an toàn lúc boot: mọi cơ cấu chấp hành phải ở mức không hoạt động. */
#define BOARD_RELAY_GPIO      4
#define BOARD_RELAY_SAFE_LVL  0

/* Kiểm tra lại với references/chip-matrix.md: không dùng strapping pin hoặc chân flash. */
